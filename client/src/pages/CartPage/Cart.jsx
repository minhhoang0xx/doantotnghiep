import React from "react";

const CartPage = () =>{
    const storageAccess = localStorage.getItem('access_token'); 
    
    if(storageAccess){

    }
    return (
        <div> Cart</div>
        )
}
 export default CartPage